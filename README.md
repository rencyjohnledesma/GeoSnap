# GeoSnap — Local Development

Simple Vite + React scaffold for GeoSnap prototype.

Getting started:

```bash
npm install
npm run dev
```

Notes:

- The app uses OpenStreetMap tiles and Nominatim for reverse geocoding. See `AGENT.md` for usage and CORS notes.
- You may need to install dependencies listed in `package.json`.
